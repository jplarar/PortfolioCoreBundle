{% extends 'PortfolioCoreBundle:Common:layout.html.twig' %}

{% block navbar %}
{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-xs-8 col-xs-offset-2">
            <div class="well">
                <h1>Introduce la matrícula del alumno</h1>
                <p>Unicamente los numeros, ejemplo 123456</p>
                <div class="row">
                    <form action="{{ path('portfolio_control_search') }}" method="post">
                        <input type="text" class="form-control" placeholder="Matricula" name="id">
                        {% if error != null %}
                            <div class="alert alert-info">
                                <p>{{ error }}</p>
                            </div>
                        {% endif %}
                        <button class="btn btn-default" type="submit">Buscar</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

{% endblock %}